name: neonpen
description: A Flutter package to add a Neon Pen style on text by changing color, opacity, layers and so on.
version: 1.0.2
homepage: https://github.com/turlvo/neonpen

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
